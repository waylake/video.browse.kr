# 📹 WebRTC Video Chat

무료 1:1 화상 채팅 서비스. 설치 없이 브라우저에서 바로 사용하는 실시간 비디오 통화 및 채팅.

## ✨ 주요 기능

- 🎥 **실시간 비디오 통화**: WebRTC 기반 P2P 연결로 저지연 고품질 통화
- 💬 **텍스트 채팅**: 비디오 통화 중 실시간 텍스트 메시지 전송
- 🎲 **랜덤 매칭**: 클릭 한 번으로 다른 사용자와 자동 매칭
- 🔐 **방 코드 공유**: 친구와 특정 방에서 만나기
- 💾 **세션 유지**: 새로고침해도 방 정보가 유지됨 (30분)
- 🌓 **다크/라이트 테마**: 원하는 테마로 전환 가능
- 📐 **레이아웃 선택**: 세로/가로 레이아웃 전환 가능
- 📺 **비디오 표시 옵션**: 레터박스(contain) 또는 꽉 채우기(cover) 선택
- 📱 **완벽한 반응형**: 모바일, 태블릿, 데스크톱 모두 지원
- ⌨️ **키보드 단축키**: 빠른 조작을 위한 단축키 지원
- ♿ **접근성**: ARIA 레이블 및 스크린 리더 지원

## 🚀 시작하기

### 필수 요구사항

- Node.js 14.0 이상
- npm 또는 yarn

### 설치

```bash
# 저장소 클론
git clone https://github.com/yourusername/webrtc-chat.git
cd webrtc-chat

# 의존성 설치
npm install

# 서버 시작
npm start
```

개발 모드 (자동 재시작):
```bash
npm run dev
```

서버가 시작되면 브라우저에서 `http://localhost:3000`을 열어주세요.

## 🎮 사용 방법

### 1. 새 방 만들기
- "새 방 만들기" 버튼 클릭
- 생성된 방 코드를 친구와 공유

### 2. 랜덤 방 참가
- "랜덤 방 참가하기" 버튼 클릭
- 자동으로 대기 중인 방에 매칭

### 3. 방 코드로 참가
- "방 코드로 참가하기" 버튼 클릭
- 친구에게 받은 7자리 코드 입력

## ⌨️ 키보드 단축키

| 단축키 | 기능 |
|--------|------|
| `Ctrl + T` | 테마 전환 (다크/라이트) |
| `Ctrl + V` | 비디오 토글 (켜기/끄기) |
| `Ctrl + M` | 오디오 토글 (음소거/해제) |
| `Ctrl + L` | 레이아웃 전환 (세로/가로) |
| `Ctrl + F` | 비디오 크기 조절 (레터박스/꽉 채우기) |
| `Ctrl + Q` | 방 나가기 |
| `Enter` | 채팅 메시지 전송 |

Mac에서는 `Ctrl` 대신 `Cmd`를 사용하세요.

## 🏗️ 기술 스택

### Frontend
- HTML5
- CSS3 (CSS Variables for theming)
- Vanilla JavaScript
- WebRTC API
- MediaDevices API

### Backend
- Node.js
- Express.js
- Socket.IO
- Compression middleware

## 📁 프로젝트 구조

```
webrtc-chat/
├── public/
│   ├── index.html          # 메인 HTML (SEO 최적화)
│   ├── robots.txt          # 검색 엔진 크롤러 설정
│   ├── sitemap.xml         # 사이트맵
│   └── manifest.json       # PWA 매니페스트
├── server.js               # Express 서버 + Socket.IO
├── package.json
└── README.md
```

## 🔧 설정

### 환경 변수

`.env` 파일을 생성하여 설정을 커스터마이징할 수 있습니다:

```env
PORT=3000
NODE_ENV=production
```

### STUN/TURN 서버

기본적으로 Google의 공개 STUN 서버를 사용합니다. 프로덕션 환경에서는 자체 TURN 서버 사용을 권장합니다.

`public/index.html`의 `config` 객체를 수정:

```javascript
const config = {
  iceServers: [
    { urls: 'stun:your-stun-server.com' },
    {
      urls: 'turn:your-turn-server.com',
      username: 'username',
      credential: 'password'
    }
  ]
};
```

## 🔒 보안

- ✅ Gzip 압축으로 대역폭 절감
- ✅ 보안 헤더 설정 (XSS, Clickjacking 방지)
- ✅ P2P 연결로 서버를 거치지 않는 미디어 스트림
- ✅ HTTPS 사용 권장 (프로덕션)
- ✅ 입력 검증 및 XSS 방지

## 🎨 커스터마이징

### 테마 색상 변경

`index.html`의 CSS 변수를 수정:

```css
:root {
  --accent-primary: #4a9eff;  /* 메인 색상 */
  --danger: #ef4444;           /* 위험 색상 */
  --success: #10b981;          /* 성공 색상 */
}
```

### 세션 유지 시간 변경

기본값은 30분입니다. `restoreSession()` 함수에서 변경:

```javascript
if (timeDiff > 30 * 60 * 1000) { // 30분
```

## 📊 SEO 최적화

이 프로젝트는 검색 엔진 최적화를 위해 다음을 포함합니다:

- ✅ 의미론적 HTML5 태그
- ✅ 메타 태그 (description, keywords)
- ✅ Open Graph 태그 (소셜 미디어)
- ✅ Twitter Card 태그
- ✅ 구조화된 데이터 (Schema.org)
- ✅ robots.txt
- ✅ sitemap.xml
- ✅ PWA manifest.json
- ✅ 접근성 (ARIA 레이블)

## 🌐 브라우저 지원

- ✅ Chrome 74+
- ✅ Firefox 66+
- ✅ Safari 12.1+
- ✅ Edge 79+
- ✅ Opera 62+

**참고**: WebRTC는 HTTPS 또는 localhost에서만 작동합니다.

## 📱 PWA (Progressive Web App)

이 앱은 PWA로 작동하여 모바일 기기에 설치할 수 있습니다:

1. 브라우저 메뉴에서 "홈 화면에 추가" 선택
2. 앱 아이콘으로 네이티브 앱처럼 실행

## 🚀 배포

### Heroku

```bash
heroku create your-app-name
git push heroku main
```

### Vercel

```bash
vercel
```

### Docker

```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install --production
COPY . .
EXPOSE 3000
CMD ["npm", "start"]
```

## 🤝 기여

기여는 언제나 환영합니다!

1. Fork the Project
2. Create your Feature Branch (`git checkout -b feature/AmazingFeature`)
3. Commit your Changes (`git commit -m 'Add some AmazingFeature'`)
4. Push to the Branch (`git push origin feature/AmazingFeature`)
5. Open a Pull Request

## 📝 라이선스

이 프로젝트는 MIT 라이선스를 따릅니다. 자세한 내용은 `LICENSE` 파일을 참조하세요.

## 🙏 감사의 말

- [WebRTC](https://webrtc.org/) - 실시간 통신 기술
- [Socket.IO](https://socket.io/) - 실시간 양방향 통신
- [Express](https://expressjs.com/) - 웹 프레임워크

## 📧 문의

프로젝트 관련 문의사항이 있으시면 [Issues](https://github.com/yourusername/webrtc-chat/issues)를 통해 알려주세요.

---

⭐ 이 프로젝트가 마음에 드셨다면 Star를 눌러주세요!

